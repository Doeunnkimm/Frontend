프론트엔드 20일차 목차

1. git, github 소개
2. git 회원가입
3. git 생성 및 저장

-----------------------------------------------------------------

git
	버전 관리 시스템(VCS, Version Control System)

git 주요 기능
	버전관리		git의 가장 기본적인 기능으로 문서의 버전(히스토리)를 관리한다.
	백업			로컬 저장소의 문서를 원격 저장소에 백업하는 기능을 제공한다.	
	협업			원격 저장소에 백업된 문서를 푸시(push)/풀(pull)하여 반복 협업이 가능하다.

github(원격 저장소의 일종)
	git을 사용할 때 백업을 위해 원격 저장소(Hub)를 제공해 주는 서비스
	git을 사용시 원격 저장소로 가장 많이 사용하지만 다른 원격 저장소를 구축하는 것도 가능하다.

git 상태
	git 파일은 항상 다음 3가지 상태 중 하나의 상태를 갖는다.
	* modified -> staged -> committed

	modified		로컬에서 파일을 수정 중인 상태
	staged		commit 되기 전 상태(임시 저장 상태)
	committed		수정 사항들이 자신의 로컬 git 환경에 저장되어 있는 상태